# Used only by the Vite development server proxy.
BACKEND_URL=http://127.0.0.1:5001

# Leave empty for same-origin /api requests. Set only for a separately hosted API.
# VITE_API_URL=https://api.example.com
